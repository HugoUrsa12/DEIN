<Window x:Class="EstilosControles.EstilosDiseño"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EstilosControles"
        mc:Ignorable="d"
        Title="EstilosDiseño" Height="450" Width="800">
    
    <!--Estilo a nivel de diseño-->
    <!--En este caso se ha utilizado un StackPanel para contener los elementos de tipo button y darles estilo-->
    <StackPanel>
        <StackPanel.Resources>
            <Style TargetType = "{x:Type Button}">
                <Setter Property = "Foreground" Value = "White" />
                <Setter Property = "Background" Value = "Orange" />
                <Setter Property = "FontStyle" Value = "Italic" />
                <Setter Property = "Width" Value = "200" />
                <Setter Property = "Height" Value = "50" />
                <Setter Property = "Margin" Value = "10" />
            </Style>
        </StackPanel.Resources>

        <Button Content="Botón 1"/>
        <Button Content="Botón 2"/>
        <Button Foreground = "Blue" Content="Botón 3"/>
    </StackPanel>
</Window>
